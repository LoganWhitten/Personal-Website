---

---

<div id="redirect"></div>

<script>
    document.addEventListener("DOMContentLoaded", (event) => {
        const isMobile = () => {
            var check = false;
            (function (a) {
                if (
                    /Android/i.test(
                        a,
                    )
                )
                    check = true;
            })(navigator.userAgent || "");
            return check;
        };

        const url = isMobile()
        ? "https://play.google.com/store/apps/details?id=com.boomapp"
        : "https://apps.apple.com/us/app/boom-not-another-sound-board/id6445970336"

        window.location.href = url;
    });
</script>
